import makeStyles from '@material-ui/styles/makeStyles';

export default makeStyles(() => ({
    root: {
        width: "100%",
        height: "85vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        fontSize: "32px",
        color: "#9e9e9e"
    }
}))